 - name: Create resource group
   hosts: localhost
   tasks:
    - name: Create a resource group
      azure_rm_resourcegroup:
        name: "{{ resource_group }}"
        location: EastAsia
        tags:
          testing: testing
          delete: never
      register: rg
    - name: print the details
      degbug:
        var: rg
      
